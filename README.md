# MESSAGE BROKER (PUBLISH AND SUBSCRIBE) USING REDIS AND NODE JS

```json
git clone https://github.com/mazufik/publish-and-subscribe-with-redis-nodejs.git


cd publish-and-subscribe-with-redis-nodejs

docker-compose up -d

npm install

npm start
```
